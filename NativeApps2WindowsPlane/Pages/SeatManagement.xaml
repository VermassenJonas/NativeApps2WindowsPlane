<Page
    x:Class="NativeApps2WindowsPlane.Pages.SeatManagement"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeApps2WindowsPlane.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:domain="using:NativeApps2WindowsPlane.Models.Domain"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <StackPanel Orientation="Vertical">

        <ListView 
            SelectionMode="Multiple" 
            ItemsSource="{Binding SeatList}"
            x:Name="SeatListView">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="domain:Seat">
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="{Binding SeatId}"/>
                        <TextBlock Text="{Binding Passenger.FullName}"/>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button>Swap Seats</Button>
    </StackPanel>

</Page>
