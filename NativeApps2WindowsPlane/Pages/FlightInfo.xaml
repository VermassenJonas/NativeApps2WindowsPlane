<Page
    x:Class="NativeApps2WindowsPlane.Pages.FlightInfo"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeApps2WindowsPlane.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="120*"/>

        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60*"></ColumnDefinition>
            <ColumnDefinition Width="60*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock FontSize="36"  Grid.Column="0"  Grid.Row="0" Text="{Binding flight.Destination}"></TextBlock>
        <TextBlock FontSize="36"  Grid.Column="0"  Grid.Row="1" Text="{Binding flight.Origin}"></TextBlock>
        <TextBlock FontSize="36"  Grid.Column="1"  Grid.Row="0" Text="{Binding flight.Departed}"></TextBlock>
        <TextBlock FontSize="36"  Grid.Column="1"  Grid.Row="1" Text="{Binding flight.ETA}"></TextBlock>
        <TextBlock FontSize="36"  Grid.Column="0"  Grid.Row="2" Grid.ColumnSpan="2" Text="{Binding weather.description}"></TextBlock>
        
    </Grid>
</Page>
