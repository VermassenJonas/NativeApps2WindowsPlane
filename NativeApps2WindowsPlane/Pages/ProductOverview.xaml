<Page
    x:Class="NativeApps2WindowsPlane.Pages.ProductOverview"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeApps2WindowsPlane.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Border>
        <StackPanel  Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock FontSize="32" Text="{x:Bind orderLine.Product.Name}"></TextBlock>
            <StackPanel Orientation="Vertical">
                <Button Click="{x:Bind AddItem}">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70E;"/>
                </Button>
                <Button Click="{x:Bind RemoveItem}">
                    <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE70D;"/>
                </Button>
            </StackPanel>
            <TextBlock FontSize="32" Text="{x:Bind orderLine.Amount}"></TextBlock>
            <Button  FontSize="32" Click="{x:Bind AddOrderLineToCart}">Add to cart</Button>
        </StackPanel>
    </Border>
</Page>
